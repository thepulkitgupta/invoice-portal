import React  from 'react';
import {Box,Typography } from '@mui/material';

export default function Home() {
    return ( 
    <Box>
        <Typography variant="h4" >No Match</Typography>
            <Typography variant='body1' >
                Path/Route not Found
            </Typography>
    </Box>
    );
}

